ENVIRONMENT=dev
DATABASE_URL=postgresql+asyncpg://cofre:cofre@localhost:5432/cofre

# Anthropic (omit in dev — agent narratives will fall back to a static message)
# ANTHROPIC_API_KEY=
ANTHROPIC_MODEL_MAIN=claude-opus-4-7
ANTHROPIC_MODEL_CHEAP=claude-haiku-4-5-20251001

# Firebase server credentials (omit in dev — auth falls back to dev user)
# FIREBASE_CREDENTIALS_PATH=/path/to/service-account.json

# SnapTrade (omit unless using brokerage sync)
# SNAPTRADE_CLIENT_ID=
# SNAPTRADE_CONSUMER_KEY=

CORS_ORIGINS=["http://localhost:5173"]
